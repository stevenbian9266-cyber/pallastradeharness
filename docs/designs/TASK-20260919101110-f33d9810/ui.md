# UI 设计 — TASK-20260919101110-f33d9810（D04/D05 Provider 抽象）

**N/A** — 本任务为引擎内部可复用层（端口定义 + 本机适配器 + 契约测试），不涉及任何界面、视图、组件或用户可见交互。

- 无前端代码变更（`presets/`、`templates/` 未改）
- 无 CLI 输出格式变更（不新增命令、不改帮助文本）
- 无 TUI 变更（`bin/tui*` 未触碰）
