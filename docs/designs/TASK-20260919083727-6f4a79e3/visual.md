# 视觉设计 — TASK-20260919083727-6f4a79e3（Batch B Template Registry）

> 适用性：**不适用（N/A）** — 无视觉产物（无截图、无组件、无设计令牌）。产出物为 Markdown 文档模板与 JSON 元数据/机读契约。

## 判定与替代约束

| 项目 | 状态 | 说明 |
|---|---|---|
| 视觉回归（visual-regression） | 不触发 | 本仓 `config.visualRegression` 未启用，且本任务无 UI |
| 设计令牌 / 配色 | 不涉及 | 模板文件不承载视觉规范 |
| 排版约定 | 轻量约束 | 模板章节标题保持 Markdown `##` 二级结构，保证可被静态解析（design-check 同思路） |

## 说明

如未来模板需要可视化预览（模板画廊），属独立任务；本批次只保证**结构与元数据**可读、可校验。
