# 视觉规范（Visual Spec）— TASK-20260830144441-f92f27de

> 设计阶段产物 3/4。对应 PRD：`PRD-20260830-other-设计检查机器校验-design-check`

## 1. 设计令牌引用

不适用（CLI 无视觉设计令牌）。控制台输出复用引擎既有风格：`✅`（通过）/`❌`（失败）/`🟡`（警告）。

## 2. 组件视觉标准

不适用（CLI）。

## 3. 响应式断点 / 布局

不适用。

## 4. 主题 / 暗色

不适用（终端原生）。

## 5. 一致性声明

输出风格与 `design:scan`、`reuse-adherence`、`baseline:check` 完全一致（图标前缀 + 中文原因 + 可执行提示），无差异。
