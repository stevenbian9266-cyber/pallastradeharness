# 视觉规范（Visual Spec）— TASK-20260918142555-5d8f7ec3

> 设计阶段产物 **3/4**。对应 PRD：`PRD-20260918-other-phase-1-mcp-零安装接入-harness-mcp-bin-root-定位-l1-工具面`
> 适用性：**不适用（N/A）** — 本任务无任何视觉产物（无页面、无截图、无样式令牌）。

## 1. 视觉基调 / 设计令牌

不适用：无颜色、字体、间距等令牌变更；仓库反模式扫描（`scan-ui-anti-patterns`）对本任务无新增约束。

## 2. 关键界面 / 组件视觉要求

不适用：MCP 工具的输出为 JSON（`structuredContent`）与纯文本 `content`，由宿主客户端渲染；本仓不控制呈现样式。

## 3. 响应式与适配

不适用。

## 4. 回归基线

不适用：无 golden 截图；验证形态为协议级端到端测试（`mcp-server.test.mjs`），非像素级比对。
