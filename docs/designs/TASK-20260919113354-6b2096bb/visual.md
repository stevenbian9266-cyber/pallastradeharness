# 视觉设计 — TASK-20260919113354-6b2096bb（本仓 strict 严格治理 dogfood）

> 无图形界面，本节承载 **CLI 视觉一致性**（颜色/符号/对齐），与既有引擎输出规范保持一致。

## 1. 视觉基调（沿用引擎既有约定）

| 元素 | 约定 | 本任务影响 |
|---|---|---|
| 成功 | `✅` 前缀 | 放行态 `✅ Task … completed with verified evidence.` 不变 |
| 失败/阻断 | `❌` 前缀 | 阻断态沿用既有 `❌ Strict finish blocked — REQUIRED_ACTIONS (N):` |
| 次级缩进 | 3 空格 + `•` 列表项、5 空格 + `→` 命令 | 既有实现，e2e 冻结 |
| 颜色 | **不引入任何 ANSI 颜色**（兼容非 TTY / CI 日志） | 零变化 |

## 2. 排版规则

1. 阻断头行必须单行可 grep（`REQUIRED_ACTIONS` 为大写常量词，e2e 以此断言）。
2. 每个缺项两行：`[id] 人类可读原因` + `→ 可执行命令`；缺项之间不空行，保持紧凑。
3. 不使用 emoji 之外的装饰字符（避免 Windows code page 乱码扩散）。

## 3. 文档视觉一致性（本任务交付的文档）

- `README.md`：新小节沿用既有 `- **能力名**（\`文件\`）：说明` 的紧凑列表风格。
- `AGENTS.md` §2：步骤以 `bash` 代码块逐行给出，与既有生命周期写法一致。
- `docs/getting-started.md`：操作步骤用有序列表 + 代码块，命令必须可直接复制。

## 4. 验收相关性

AC-002 / AC-003 / AC-005 断言的是**文本形状**（常量词 + 命令），因此视觉约定即为可测约定。
