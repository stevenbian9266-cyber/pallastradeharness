# deploy/.env.mcp.example — 复制为 deploy/.env.mcp 并填真实值（服务器上 chmod 600）
#
# 本文件是模板（入库）；.env.mcp 是真实文件（**不入库**）。
# 与 RFC-0005 附录 C 的差异：鉴权键按已实现形态改为单静态 Key（HARNESS_API_KEY），
# 不再使用 HARNESS_CLOUD_TOKEN_SALT / HARNESS_CLOUD_ADMIN_KEY（那是 M5 多 Key 形态）。

# 必填：MCP 静态 Key（Bearer）。缺失时服务**拒绝启动**。
# 生成：openssl rand -hex 32
HARNESS_API_KEY=<random-64hex>

# 监听端口（compose 端口映射与 nginx 反代目标必须一致）
PORT=3110

# 容器内数据目录（compose 挂载 ../data 到此）
DATA_DIR=/data

NODE_ENV=production

# 可选：SQLite 文件路径（默认 $DATA_DIR/harness-cloud.db）
# HARNESS_CLOUD_DB=/data/harness-cloud.db

# 可选：设置后服务会加载该目录的 harness.config.mjs；不设置则使用引擎默认配置
# HARNESS_ROOT=
