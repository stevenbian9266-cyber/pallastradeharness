# /etc/nginx/conf.d/mcp-ratelimit.conf
# 仅供 mcp.pallastrade.cn 使用的独立限流 zone（RFC-0005 §3.6 / §6.5）。
# 放在 conf.d（http context）——不能在 sites-available 里定义 zone。
limit_req_zone $binary_remote_addr zone=mcp_rl:10m rate=2r/s;
