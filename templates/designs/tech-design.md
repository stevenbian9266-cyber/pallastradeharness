# 技术方案（Tech Design）— <task-id>

> 设计阶段产物 **4/4（核心）**。对应 PRD：`<PRD-id>`；Task：`<task-id>`
> 目的：先识别现状、再定复用决策，防止重复造轮子与改错归属模块。
> 前置：先运行 `harness design:scan --scope all`，将输出作为 Part A 的事实来源（不得臆造现状）。

## Part A — 现状识别（强制，缺此段方案无效）

### A1 业务系统盘点

| 现有业务模块/服务 | 边界 | 新功能归属 |
|---|---|---|
（事实来源：`harness design:scan --scope business`）

### A2 数据模型识别

| 现有表/集合/模型 | 关联 | 新增 or 扩展 |
|---|---|---|
（事实来源：`harness design:scan --scope data`）

### A3 字段盘点

| 字段 | 现有定义（类型/约束） | 新增 or 复用 |
|---|---|---|
|  |  |  |

### A4 代码结构

| 公共方法/组件/工具 | 位置 | 签名/说明 |
|---|---|---|
（事实来源：`harness design:scan --scope code`）

## Part B — 复用决策矩阵（每个能力需求必须二选一；决策列限：调用已有 / 扩展已有 / 新封装公用 / 新建局部）

| 能力需求 | 决策 | 目标 | 依据（已有位置/签名） |
|---|---|---|---|
| 示例：日期格式化 | 调用已有 | formatDate | src/lib/date.ts:12 |
|  |  |  |  |

## Part C — 实施落点

### C1 新增/修改文件清单

| 文件路径 | 操作（新增/修改） | 说明 |
|---|---|---|
|  |  |  |

### C2 分层改动

- UI 层：
- 业务层：
- 数据层：

### C3 依赖与实施顺序

1. 

### C4 风险与回滚

- 
