# Engineering Standard

> Project Constitution 模板（Batch B / B08）。定位：**项目工程原则（人可读）**。
> 与机器规则分工：`rules/base-standards.json` / `harness/standards/**` 是**机器可执行规则**（Standard schema）；
> 本文档定原则与取舍，机器规则定可自动校验的条目。两者冲突时：机器规则负责"卡"，本文档负责"解释为什么"。

## Directory Rules

- 目录职责与禁止事项（哪些目录不得放什么）：
- 新增顶层目录的审批要求：

## Naming

- 文件 / 目录 / 变量 / 导出符号命名约定：
- 领域术语与代码命名的映射：

## Module Organization

- 模块拆分原则（按领域 / 按层 / 按变化频率）：
- 模块公开面（导出什么、隐藏什么）：

## Reuse First

- 实施前必须做的复用检索（跨层搜索范围与关键词策略）：
- 允许的复用粒度（调用 / 扩展 / 封装）：

## Do Not Duplicate

- 禁止的重复形态（复制粘贴、第二套引擎、并列实现）：
- 发现重复时的处置（合并 / 抽象 / 记录例外）：

## Error Handling

- 错误分类与传播策略（抛出 / 返回 / 降级）：
- 禁止静默吞错（禁止把未知错误折叠为空结果）：

## Validation

- 输入校验边界与失败语义：
- 校验必须复用的既有工具：

## Logging

- 日志级别 / 格式 / 敏感信息遮蔽要求：
- 禁止的日志内容：

## Configuration

- 配置来源优先级与默认值政策：
- 禁止把环境差异硬编码进领域逻辑：

## Dependency Policy

- 新增依赖的门槛（必要性 / 维护性 / 供应链）：
- 禁止重复能力的依赖（同一问题不得引入第二个库）：

## Type Safety

- 类型 / 契约边界的强制程度：
- 动态数据（JSON / 外部输入）的校验入口：

## Refactoring Policy

- 允许的重构时机（独立任务 / 随功能 / 禁止顺手重构）：
- 行为不变的证据要求：

## Performance

- 关键路径性能预算与测量方式：
- 禁止的过早优化与明显的退化模式：

## Security Baseline

- 密钥 / 凭据 / 权限 / 注入的最小基线：
- 安全相关变更的审批要求：

## Forbidden Patterns

- 项目级别禁止的编码模式（与反模式扫描规则对齐）：

## Definition of Done

- 工程完成的通用判据（测试 / 证据 / 知识同步 / 审查）：
- 例外与豁免的申请方式：
